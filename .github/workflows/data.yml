name: V38 Legacy Data Workflow (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Canonical pipeline notice
        run: |
          echo 'This legacy acquisition workflow is intentionally disabled.'
          echo 'Use "V38 Production Full Pipeline" for fetch -> calculate -> validate -> build -> publish.'
